# Este programa calcula a área de um triângulo
# Recebe o valor da base
base = float(input('valor da base: '))
# Recebe o valor da altura
altura = float(input('valor da altura: '))
# Calcula a área do triângulo
area = (base*altura)/2
# Exibe o resultado da área
print(area)